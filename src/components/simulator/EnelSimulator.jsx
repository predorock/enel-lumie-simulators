
const EnelSimulator = (
    // The pages json to load
    pages,
    // The configurable steps of the simulation (like a breadcrumb)
    steps,
    // The registry of components
    registry
) => {




    return null
};

export default EnelSimulator;
