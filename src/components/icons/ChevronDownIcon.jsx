import { ChevronDownIcon as RadixChevronDownIcon } from '@radix-ui/react-icons';

// Chevron down icon for buttons and links using Radix UI
export default function ChevronDownIcon({ className = "w-4 h-4" }) {
    return (
        <RadixChevronDownIcon className={className} />
    );
}
