import { ChevronUpIcon as RadixChevronUpIcon } from '@radix-ui/react-icons';

// Chevron down icon for buttons and links using Radix UI
export default function ChevronUpIcon({ className = "w-4 h-4" }) {
    return (
        <RadixChevronUpIcon className={className} />
    );
}
