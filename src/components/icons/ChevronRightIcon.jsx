import { ChevronRightIcon as RadixChevronRightIcon } from '@radix-ui/react-icons';

// Chevron right icon for buttons and links using Radix UI
export default function ChevronRightIcon({ className = "w-4 h-4" }) {
    return (
        <RadixChevronRightIcon className={className} />
    );
}
